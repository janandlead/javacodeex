RewriteEngine On

# Preserve real files such as assets, robots.txt, sitemap.xml, and favicon.ico.
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Let Angular Router resolve every public client-side route on refresh.
RewriteRule ^ index.html [L]
