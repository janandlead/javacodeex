import { InterviewQuestion } from '../models/interview-question.model';

const exceptionQuestion = (id: number, question: string, answer: string, difficulty: InterviewQuestion['difficulty'], tags: readonly string[], codeExample?: string): InterviewQuestion => ({ id, topicId: 'java', section: 'exception-handling', question, answer, difficulty, tags, codeExample });

export const JAVA_EXCEPTION_QUESTIONS: readonly InterviewQuestion[] = [
  exceptionQuestion(91, 'What is an exception in Java?', 'An exception is an unexpected event that interrupts normal program flow. Java creates an exception object containing its type, message, location, and call sequence.', 'Beginner', ['Exception', 'Basics'], 'int result = 10 / 0; // ArithmeticException'),
  exceptionQuestion(92, 'What is exception handling?', 'Exception handling detects runtime problems and responds gracefully using try, catch, finally, throw, and throws.', 'Beginner', ['try', 'catch', 'finally']),
  exceptionQuestion(93, 'Why is exception handling important?', 'It prevents sudden termination, provides useful messages, separates error handling from business logic, releases resources, and supports logging and monitoring.', 'Beginner', ['Reliability', 'Logging']),
  exceptionQuestion(94, 'What is the exception hierarchy in Java?', 'Throwable is the root of Error and Exception. RuntimeException represents unchecked exceptions, while IOException, SQLException, and ClassNotFoundException are common checked exceptions.', 'Intermediate', ['Throwable', 'Hierarchy'], 'Object -> Throwable -> Error\n                    -> Exception -> RuntimeException'),
  exceptionQuestion(95, 'What is the difference between an exception and an error?', 'Exceptions are usually application-level problems that may be recoverable. Errors normally indicate serious JVM or system failures such as OutOfMemoryError and should rarely be handled.', 'Beginner', ['Error', 'Exception']),
  exceptionQuestion(96, 'What are checked exceptions?', 'Checked exceptions are verified by the compiler. The caller must handle them with try-catch or declare them with throws. IOException and SQLException are examples.', 'Beginner', ['Checked exception', 'IOException']),
  exceptionQuestion(97, 'What are unchecked exceptions?', 'Unchecked exceptions extend RuntimeException and are not enforced by the compiler. They commonly represent invalid input, programming errors, or invalid state.', 'Beginner', ['Unchecked exception', 'RuntimeException']),
  exceptionQuestion(98, 'What is the difference between checked and unchecked exceptions?', 'Checked exceptions must be handled or declared and often represent external failures. Unchecked exceptions occur at runtime and commonly represent invalid code, input, or state.', 'Beginner', ['Checked', 'Unchecked']),
  exceptionQuestion(99, 'What is a try block?', 'A try block contains code that may throw an exception and must be followed by catch, finally, or both.', 'Beginner', ['try', 'Syntax'], 'try { int result = 100 / 0; } finally { cleanup(); }'),
  exceptionQuestion(100, 'What is a catch block?', 'A catch block handles an exception thrown by its associated try block and can inspect methods such as getMessage, getCause, and printStackTrace.', 'Beginner', ['catch', 'Handling']),
  exceptionQuestion(101, 'What is a finally block?', 'finally contains cleanup code that normally runs whether the try succeeds or an exception is caught. Try-with-resources is preferred for closeable resources.', 'Beginner', ['finally', 'Cleanup']),
  exceptionQuestion(102, 'Is the finally block always executed?', 'It normally executes, but not after System.exit, an unexpected JVM or operating-system termination, or a fatal process failure.', 'Intermediate', ['finally', 'System.exit']),
  exceptionQuestion(103, 'Can we use try without catch?', 'Yes. A try block may be followed by finally without catch. The exception is still propagated after cleanup.', 'Beginner', ['try', 'finally'], 'try { process(); } finally { release(); }'),
  exceptionQuestion(104, 'Can we use catch without try?', 'No. Every catch block must be associated with a try block, otherwise the code does not compile.', 'Beginner', ['catch', 'Syntax']),
  exceptionQuestion(105, 'Can we use multiple catch blocks?', 'Yes. Multiple catch blocks allow different exception types to receive different recovery messages or actions.', 'Beginner', ['Multiple catch'], 'try { process(); } catch (NumberFormatException e) { } catch (ArithmeticException e) { }'),
  exceptionQuestion(106, 'What is the correct order of multiple catch blocks?', 'Specific exception types must appear before general types. A catch(Exception) before catch(IOException) makes the latter unreachable.', 'Intermediate', ['Catch order', 'Inheritance']),
  exceptionQuestion(107, 'What is multi-catch in Java?', 'Multi-catch handles unrelated exception types in one catch block when their handling logic is identical. It was introduced in Java 7.', 'Beginner', ['Multi-catch', 'Java 7'], 'catch (NumberFormatException | ArithmeticException exception) {\n  log.warn("Invalid number", exception);\n}'),
  exceptionQuestion(108, 'Can related exceptions be used in the same multi-catch?', 'No. Multi-catch alternatives cannot have a parent-child relationship, such as IOException and FileNotFoundException. Use the parent type instead.', 'Intermediate', ['Multi-catch', 'Inheritance']),
  exceptionQuestion(109, 'What is the throw keyword?', 'throw explicitly creates and raises one exception object, usually when input or a business rule is invalid.', 'Beginner', ['throw', 'Validation'], 'if (age < 18) throw new IllegalArgumentException("Age must be at least 18");'),
  exceptionQuestion(110, 'What is the throws keyword?', 'throws declares checked exceptions that a method may pass to its caller. The caller must handle or further declare them.', 'Beginner', ['throws', 'Methods'], 'String readFile(String path) throws IOException { return Files.readString(Path.of(path)); }'),
  exceptionQuestion(111, 'What is the difference between throw and throws?', 'throw raises an exception object inside code. throws declares exception types in a method signature and can list multiple types.', 'Beginner', ['throw', 'throws']),
  exceptionQuestion(112, 'What is exception propagation?', 'Exception propagation passes an unhandled exception up the method-call stack until a compatible handler is found or the JVM default handler terminates the thread.', 'Intermediate', ['Propagation', 'Call stack']),
  exceptionQuestion(113, 'Do checked and unchecked exceptions both propagate?', 'Yes. Both can move through method calls. Checked exceptions must be handled or declared at required boundaries, while unchecked exceptions do not require declaration.', 'Intermediate', ['Propagation', 'Checked']),
  exceptionQuestion(114, 'What is a custom exception?', 'A custom exception is a domain-specific exception created to express a business or application failure clearly.', 'Beginner', ['Custom exception', 'Domain'], 'class InsufficientBalanceException extends RuntimeException {\n  InsufficientBalanceException(String message) { super(message); }\n}'),
  exceptionQuestion(115, 'How do you create a checked custom exception?', 'Extend Exception and declare or handle it at the call site.', 'Beginner', ['Custom exception', 'Checked'], 'class InvalidFileFormatException extends Exception {\n  InvalidFileFormatException(String message) { super(message); }\n}'),
  exceptionQuestion(116, 'How do you create an unchecked custom exception?', 'Extend RuntimeException. The compiler does not force callers to handle or declare it.', 'Beginner', ['Custom exception', 'RuntimeException'], 'class EmployeeNotFoundException extends RuntimeException { }'),
  exceptionQuestion(117, 'When should checked exceptions be used?', 'Use them when a caller can reasonably recover from an external condition such as a missing file, failed network connection, or unavailable resource.', 'Intermediate', ['Checked exception', 'Recovery']),
  exceptionQuestion(118, 'When should unchecked exceptions be used?', 'Use them for invalid arguments, violated business rules, missing entities, invalid application state, or failures that the current layer cannot meaningfully recover from.', 'Intermediate', ['Unchecked exception', 'Business rules']),
  exceptionQuestion(119, 'What is try-with-resources?', 'Try-with-resources automatically closes resources implementing AutoCloseable or Closeable after the try block completes.', 'Beginner', ['Try-with-resources', 'AutoCloseable'], 'try (BufferedReader reader = Files.newBufferedReader(path)) {\n  return reader.readLine();\n}'),
  exceptionQuestion(120, 'What are the benefits of try-with-resources?', 'It reduces boilerplate, prevents resource leaks, closes resources reliably, and handles cleanup exceptions more safely than manual finally code.', 'Beginner', ['Resources', 'Cleanup']),
  exceptionQuestion(121, 'Can multiple resources be declared in try-with-resources?', 'Yes. Resources are closed automatically in reverse order of declaration.', 'Beginner', ['Resources', 'Files'], 'try (Reader reader = openReader(); Writer writer = openWriter()) { copy(reader, writer); }'),
  exceptionQuestion(122, 'What is the AutoCloseable interface?', 'AutoCloseable marks a resource that can be closed automatically. Its close method may throw Exception and is invoked by try-with-resources.', 'Beginner', ['AutoCloseable', 'Resources'], 'class DatabaseSession implements AutoCloseable {\n  public void close() { }\n}'),
  exceptionQuestion(123, 'What is a suppressed exception?', 'If the try body throws and close also throws, the body exception is primary and the close exception is attached as suppressed through getSuppressed().', 'Advanced', ['Suppressed exception', 'Resources']),
  exceptionQuestion(124, 'What happens when try and finally both return?', 'The return from finally overrides the return from try. Returning from finally is bad practice because it can hide exceptions and results.', 'Advanced', ['finally', 'return'], 'static int value() { try { return 10; } finally { return 20; } }'),
  exceptionQuestion(125, 'What happens when finally throws an exception?', 'An exception from finally can hide an exception from try or catch. Cleanup code should avoid throwing unless there is no safe alternative.', 'Advanced', ['finally', 'Exception masking']),
  exceptionQuestion(126, 'What is exception chaining?', 'Exception chaining wraps a low-level exception in a higher-level exception while preserving the original cause for debugging.', 'Intermediate', ['Chaining', 'Cause'], 'throw new DataAccessException("Unable to load data", sqlException);'),
  exceptionQuestion(127, 'Why should the original exception cause be preserved?', 'The cause preserves the original stack trace, root-cause details, and debugging context needed for production diagnosis.', 'Intermediate', ['Cause', 'Debugging'], 'throw new RuntimeException("Database failed", exception);'),
  exceptionQuestion(128, 'What is a stack trace?', 'A stack trace shows the exception type and the sequence of method calls, classes, files, and line numbers that led to the failure.', 'Beginner', ['Stack trace', 'Debugging']),
  exceptionQuestion(129, 'What is the difference between getMessage and printStackTrace?', 'getMessage returns only the exception message. printStackTrace prints the complete call sequence and should normally be replaced by a logging framework in production.', 'Beginner', ['Logging', 'Stack trace']),
  exceptionQuestion(130, 'Why should printStackTrace be avoided in production?', 'It writes unstructured output directly to standard error, may bypass centralized logging, cannot be controlled by log level, and may expose sensitive details.', 'Intermediate', ['Logging', 'Security']),
  exceptionQuestion(131, 'What is the difference between final, finally, and finalize?', 'final prevents reassignment, overriding, or inheritance. finally provides cleanup flow. finalize is a deprecated cleanup callback and must not be used for resources.', 'Beginner', ['final', 'finally', 'finalize']),
  exceptionQuestion(132, 'Can we catch Throwable?', 'Technically yes, but it also catches serious Errors such as OutOfMemoryError and StackOverflowError. Catch specific exceptions instead.', 'Advanced', ['Throwable', 'Error']),
  exceptionQuestion(133, 'Can we catch Error?', 'Technically yes, but catching Error does not guarantee safe recovery. Most Errors represent severe JVM or system failures.', 'Advanced', ['Error', 'Recovery']),
  exceptionQuestion(134, 'Why is catching Exception everywhere bad practice?', 'It handles unrelated failures together, hides programming errors, weakens recovery decisions, and makes debugging harder. Catch specific types instead.', 'Intermediate', ['Exception', 'Best practices']),
  exceptionQuestion(135, 'What is exception swallowing?', 'Exception swallowing is catching an exception without logging, handling, or rethrowing it. It makes failures invisible and should be avoided.', 'Beginner', ['Swallowing', 'Logging'], 'catch (PaymentException exception) {\n  log.error("Payment failed", exception);\n  throw exception;\n}'),
  exceptionQuestion(136, 'Should exceptions be used for normal program flow?', 'No. Validate predictable conditions normally and use exceptions for exceptional failures. Frequent exceptions create unnecessary stack traces and obscure intent.', 'Intermediate', ['Validation', 'Performance']),
  exceptionQuestion(137, 'Can an overriding method throw a checked exception?', 'It can throw the same checked exception, a narrower subclass, or none. It cannot throw a broader checked exception than the parent method.', 'Advanced', ['Overriding', 'Checked exception']),
  exceptionQuestion(138, 'Can an overriding method throw an unchecked exception?', 'Yes. Unchecked exceptions are not restricted by the checked-exception overriding rules.', 'Intermediate', ['Overriding', 'RuntimeException']),
  exceptionQuestion(139, 'Can a constructor throw an exception?', 'Yes. A constructor can throw checked or unchecked exceptions. If it fails, the object is not created successfully.', 'Beginner', ['Constructor', 'Validation'], 'Employee(String name) {\n  if (name == null) throw new IllegalArgumentException();\n}'),
  exceptionQuestion(140, 'Can a static initialization block throw an exception?', 'It can throw unchecked exceptions. A failure during class initialization may be reported as ExceptionInInitializerError.', 'Advanced', ['Static initialization', 'Class loading']),
  exceptionQuestion(141, 'How are exceptions handled in Spring Boot?', 'They can be handled locally, with @ExceptionHandler, globally with @RestControllerAdvice, or translated into custom structured API errors. Centralized handling is preferred for REST APIs.', 'Intermediate', ['Spring Boot', 'REST']),
  exceptionQuestion(142, 'What is @ExceptionHandler in Spring Boot?', '@ExceptionHandler maps a specified exception to a handler method in a controller or controller advice class.', 'Beginner', ['Spring Boot', '@ExceptionHandler'], '@ExceptionHandler(EmployeeNotFoundException.class)\nResponseEntity<ApiError> handle(EmployeeNotFoundException ex) { return ResponseEntity.notFound().build(); }'),
  exceptionQuestion(143, 'What is @ControllerAdvice?', '@ControllerAdvice centralizes shared controller behavior, including exception handlers, across multiple controllers.', 'Beginner', ['Spring Boot', '@ControllerAdvice']),
  exceptionQuestion(144, 'What is @RestControllerAdvice?', '@RestControllerAdvice combines controller advice with response-body behavior and is commonly used for global REST API exception handling.', 'Beginner', ['Spring Boot', '@RestControllerAdvice']),
  exceptionQuestion(145, 'How do you create a standard API error response?', 'Use a consistent object containing timestamp, status, safe error code, message, path, correlation ID, and validation field errors.', 'Intermediate', ['API errors', 'REST'], 'public record ApiError(Instant timestamp, int status, String errorCode, String message, String path) {}'),
  exceptionQuestion(146, 'How should a not-found exception be handled in Spring Boot?', 'Throw a custom EmployeeNotFoundException from the service, handle it in global advice, and return a safe 404 response with a stable error code.', 'Beginner', ['Spring Boot', '404']),
  exceptionQuestion(147, 'How are validation exceptions handled in Spring Boot?', 'Handle MethodArgumentNotValidException in global advice, collect field errors, and return a structured 400 or 422 response.', 'Intermediate', ['Validation', 'Spring Boot']),
  exceptionQuestion(148, 'What is the difference between business and system exceptions?', 'Business exceptions represent known rule violations and usually map to 4xx responses. System exceptions represent technical failures and usually map to 5xx responses.', 'Intermediate', ['Business exception', 'System exception']),
  exceptionQuestion(149, 'Should controllers contain large try-catch blocks?', 'Generally no. Keep controllers focused on HTTP mapping and let global advice translate service and domain exceptions consistently.', 'Beginner', ['Controllers', 'Global handling']),
  exceptionQuestion(150, 'What HTTP status codes are commonly used for exceptions?', '400 represents invalid input, 401 authentication, 403 access denial, 404 missing resources, 409 conflicts, 429 rate limits, 500 unexpected errors, and 503 unavailable dependencies.', 'Beginner', ['HTTP', 'REST']),
  exceptionQuestion(151, 'How should exceptions be handled between microservices?', 'Return safe structured errors with status, business code, message, timestamp, path, and correlation ID. Keep stack traces and internal details in secured server logs.', 'Intermediate', ['Microservices', 'Correlation ID']),
  exceptionQuestion(152, 'How do you handle downstream service failures?', 'Configure timeouts, classify client errors, retry only transient failures, use circuit breakers, preserve causes, log trace IDs, and avoid exposing downstream internals.', 'Advanced', ['Microservices', 'Resilience']),
  exceptionQuestion(153, 'Should every exception be retried?', 'No. Retry transient timeouts or temporary unavailability with bounded exponential backoff. Do not retry validation, authentication, not-found, or non-idempotent failures without protection.', 'Advanced', ['Retry', 'Resilience']),
  exceptionQuestion(154, 'What is the role of a correlation ID?', 'A correlation ID connects logs for one request across gateways, services, databases, Kafka, and downstream calls, making distributed failures traceable.', 'Intermediate', ['Correlation ID', 'Observability']),
  exceptionQuestion(155, 'How should Kafka consumer exceptions be handled?', 'Classify transient and permanent failures, use bounded retry with backoff, dead-letter topics, idempotent processing, acknowledgements, and monitoring for retry and dead-letter counts.', 'Advanced', ['Kafka', 'Consumer']),
  exceptionQuestion(156, 'What is a dead-letter topic?', 'A dead-letter topic stores messages that still fail after configured retries so the main consumer can continue and operators can investigate or replay them.', 'Intermediate', ['Kafka', 'Dead letter']),
  exceptionQuestion(157, 'What is a poison-pill message?', 'A poison-pill message repeatedly fails because of invalid data, schema, or deserialization. It should be isolated after bounded retries instead of blocking the consumer forever.', 'Intermediate', ['Kafka', 'Poison pill']),
  exceptionQuestion(158, 'How should database exceptions be handled?', 'Translate persistence failures into meaningful application exceptions, log safe context, propagate to global handling, and never expose SQL, credentials, schema details, or vendor messages.', 'Advanced', ['Database', 'Data access']),
  exceptionQuestion(159, 'What is exception translation in Spring?', 'Spring translates vendor-specific database failures into a consistent DataAccessException hierarchy, such as DuplicateKeyException and DataIntegrityViolationException.', 'Advanced', ['Spring', 'DataAccessException']),
  exceptionQuestion(160, 'How do exceptions affect Spring transactions?', 'By default, Spring rolls back for RuntimeException and Error but not for checked exceptions.', 'Advanced', ['Transactions', 'Spring'], '@Transactional\npublic void createOrder(Order order) {\n  repository.save(order);\n  throw new IllegalStateException("Failed");\n}'),
  exceptionQuestion(161, 'How do you roll back a Spring transaction for a checked exception?', 'Use @Transactional(rollbackFor = IOException.class) or another explicitly configured exception type.', 'Advanced', ['Transactions', 'rollbackFor'], '@Transactional(rollbackFor = IOException.class)\npublic void importOrders() throws IOException { }'),
  exceptionQuestion(162, 'What happens when an exception is caught inside a transactional method?', 'If a runtime exception is caught and swallowed, Spring may commit the transaction. Rethrow it or explicitly mark rollback when failure should undo the work.', 'Advanced', ['Transactions', 'Rollback']),
  exceptionQuestion(163, 'How would you handle an employee-not-found scenario?', 'Throw a custom unchecked exception in the service, map it globally, and return a safe 404 Not Found response.', 'Beginner', ['Scenario', '404']),
  exceptionQuestion(164, 'How would you handle a duplicate employee email?', 'Enforce a database unique constraint, translate DataIntegrityViolationException, and return 409 Conflict. A pre-check alone is unsafe under concurrency.', 'Advanced', ['Database', '409']),
  exceptionQuestion(165, 'How would you handle an external API timeout?', 'Configure timeouts, retry safely with backoff, use a circuit breaker, preserve the cause, log correlation details, and return a safe service-unavailable response.', 'Advanced', ['Timeout', 'Circuit breaker']),
  exceptionQuestion(166, 'How would you handle exceptions in a scheduled job?', 'Catch failures at the job boundary, log job and execution IDs, record metrics, isolate item failures when appropriate, retry transient work, and notify support for critical failures.', 'Intermediate', ['Scheduling', 'Jobs']),
  exceptionQuestion(167, 'How would you handle an exception in asynchronous code?', 'Observe CompletableFuture or reactive failures with exceptionally, handle, or an equivalent error path. Do not allow asynchronous failures to disappear silently.', 'Advanced', ['Async', 'CompletableFuture'], 'CompletableFuture.supplyAsync(service::load).exceptionally(error -> { log.error("Async failed", error); return List.of(); });'),
  exceptionQuestion(168, 'How would you design exception handling for a layered application?', 'Repositories handle persistence details, services enforce business rules, controllers remain thin, global advice maps errors to responses, and logging/monitoring captures technical context.', 'Advanced', ['Architecture', 'Layers']),
  exceptionQuestion(169, 'What happens if no matching catch block is found?', 'The exception propagates to the caller. If nobody handles it, the JVM default handler prints details and terminates the affected thread.', 'Beginner', ['Propagation', 'JVM']),
  exceptionQuestion(170, 'What happens if an exception occurs inside a catch block?', 'The new exception propagates unless another surrounding try-catch handles it. The original handler does not automatically handle failures created inside itself.', 'Intermediate', ['catch', 'Propagation']),
  exceptionQuestion(171, 'Can one exception handler handle multiple exception types?', 'Yes, multi-catch can handle unrelated exception types in one block when their handling logic is the same.', 'Beginner', ['Multi-catch', 'Handling']),
  exceptionQuestion(172, 'Can we rethrow an exception?', 'Yes. A handler can log and rethrow the same exception or wrap it in a higher-level exception while preserving the cause.', 'Beginner', ['Rethrow', 'Cause'], 'catch (PaymentException ex) { log.error("Payment failed", ex); throw ex; }'),
  exceptionQuestion(173, 'Can a method declare an unchecked exception using throws?', 'Yes, but it is optional because the compiler does not require unchecked exceptions to be declared. It may still document the method contract.', 'Beginner', ['throws', 'RuntimeException']),
  exceptionQuestion(174, 'Can the main method throw an exception?', 'Yes. An unhandled exception declared by main reaches the JVM default handler and normally terminates the application.', 'Beginner', ['main', 'throws'], 'public static void main(String[] args) throws IOException { }'),
  exceptionQuestion(175, 'Can we have nested try-catch blocks?', 'Yes, but excessive nesting makes control flow hard to understand. Prefer focused methods and specific handlers.', 'Beginner', ['Nested try', 'Readability']),
  exceptionQuestion(176, 'What are best practices for exception handling?', 'Catch specific exceptions, avoid empty catches, preserve causes, use custom domain errors, prefer try-with-resources, avoid finally returns, log safely, centralize REST handling, and distinguish retryable failures.', 'Advanced', ['Best practices', 'Production']),
  exceptionQuestion(177, 'Should we log and rethrow the same exception?', 'Usually not at every layer. Add context when wrapping and log once at the boundary that has enough business and technical context.', 'Intermediate', ['Logging', 'Rethrow']),
  exceptionQuestion(178, 'What information should be included in exception logs?', 'Include exception and stack trace, correlation and trace IDs, operation, safe business identifiers, downstream service, retry count, and duration. Never log credentials, tokens, or unmasked sensitive data.', 'Advanced', ['Logging', 'Security']),
  exceptionQuestion(179, 'What should not be returned to API clients?', 'Do not return stack traces, SQL, class names, file paths, internal addresses, credentials, tokens, or raw downstream errors. Return a safe structured error instead.', 'Beginner', ['API security', 'REST'])
];
